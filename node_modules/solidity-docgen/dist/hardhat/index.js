"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const config_1 = require("hardhat/config");
require("./type-extensions");
(0, config_1.extendConfig)((config, userConfig) => {
    var _a;
    (_a = config.docgen) !== null && _a !== void 0 ? _a : (config.docgen = {});
    config.docgen.root = config.paths.root;
});
(0, config_1.task)('docgen', async (_, hre) => {
    await hre.run('compile');
    const { promises: fs } = await Promise.resolve().then(() => __importStar(require('fs')));
    const { main } = await Promise.resolve().then(() => __importStar(require('../main')));
    const buildInfoPaths = await hre.artifacts.getBuildInfoPaths();
    const builds = await Promise.all(buildInfoPaths.map(async (p) => JSON.parse(await fs.readFile(p, 'utf8'))));
    await main(builds, hre.config.docgen);
});
//# sourceMappingURL=index.js.map